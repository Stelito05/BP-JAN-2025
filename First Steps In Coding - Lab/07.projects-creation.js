function projectsCreation(name, projectsCount) {
    let neededHours = projectsCount * 3;
    console.log(`The architect ${name} will need ${neededHours} hours to complete ${projectsCount} project/s.`);
}

projectsCreation("George", 4);